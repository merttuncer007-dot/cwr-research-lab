---
name: cwr-research-lab
description: Continue or rehydrate the Cold War Renaissance research lab from its local SQLite archive, preserving exactness, provenance, collision corrections, byproducts, and frontier state. Use for CWR research waves, archive recovery, paper ingestion, or transferring the methodology to a new domain.
---

# CWR Research Lab

Treat the chat as a temporary client of the persistent lab.

Before continuing research, read the workspace `LAB_KERNEL.md` and `LAB_STATE.md`, run `cwr_lab.py doctor`, then generate a query-specific context pack with `cwr_lab.py context --query ...`. Do not read the full transcript unless recovering provenance or missing canonical material.

For a research wave, follow `LAB_WAVE_PROTOCOL.md`. Preserve these non-obvious invariants:

- attempt the cheapest destructive test before investing in a proof;
- distinguish proof, exact symbolic check, numerical stress, and conjecture;
- distinguish source discovery, scoped reading, and cover-to-cover reading;
- run ownership/collision audit after a candidate mechanism exists;
- downgrade collided novelty without deleting the useful theorem, application, or correction;
- harvest independent-value results as append-only byproducts;
- record failed branches and missing artifacts;
- update state and take a checkpoint before handoff.

When transferring the method to another domain, retain the epistemic protocol and provenance model, but create a new domain namespace, vocabulary, frontier, and source ledger. Do not copy CWR mathematical claims into an unrelated domain as assumptions.

Autonomous macros are disabled unless the user explicitly authorizes a later automation stage and its stopping/approval conditions.
